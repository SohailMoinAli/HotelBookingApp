@* @model HotelBookingApp.Models.Reservation

<h2>Booking Confirmation</h2>

@if (Model != null)
{
    <div class="reservation-details">
        <h3>Thank you @Model.CustomerName for your booking!</h3>
        <p>Your reservation ID <strong>@Model.ReservationId</strong> is confirmed.</p>
        <div>
            <strong>Room Number:</strong> @Model.Room.RoomNumber<br>
            <strong>Room Type:</strong> @Model.Room.Type<br>
            <strong>Check-in Date:</strong> @Model.CheckInDate.ToString("MMMM dd, yyyy")<br>
            <strong>Check-out Date:</strong> @Model.CheckOutDate.ToString("MMMM dd, yyyy")<br>
            <strong>Total Cost:</strong> $@Model.TotalPrice.ToString("C")
        </div>
       
    </div>

    <a href="@Url.Action("Index", "Home")" class="btn btn-primary">Back to Home</a>
}
else
{
    <p>Reservation details could not be loaded. Please contact support if you think this is an error.</p>
}

<style>
    .reservation-details {
        border: 1px solid #ccc;
        padding: 20px;
        margin-top: 20px;
        border-radius: 8px;
        background-color: #f9f9f9;
    }
    .btn {
        padding: 10px 20px;
        color: white;
        background-color: #007bff;
        border: none;
        border-radius: 5px;
        text-decoration: none;
        display: inline-block;
    }
    .btn-primary:hover {
        background-color: #0056b3;
    }
</style>
 *@